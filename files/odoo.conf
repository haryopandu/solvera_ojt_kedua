[options]
; ===== Database (pakai db_uri saja; hapus baris db_host/db_user/db_password/db_port) =====
db_uri = postgresql://odoo:admin@127.0.0.1:5432/odoo18   ; ID: user 'odoo', pass 'admin', DB 'odoo18' (lokal PG17)

; ===== Addons =====
addons_path = /home/haryo/work/odoo/addons,/home/haryo/custom_addons  ; ID: core + custom

; ===== Odoo master password (untuk manajemen DB via UI) =====
admin_passwd = admin   ; ID: ganti ke nilai kuat untuk kerja nyata

; ===== Logging & dev =====
log_level = info
dev = reload,qweb,assets

; --- SMTP (Gmail dengan App Password) ---
smtp_server = smtp.gmail.com         ; ID: host Gmail
smtp_port = 587                      ; ID: port STARTTLS
smtp_encryption = starttls           ; ID: Odoo 17/18: gunakan starttls pada port 587
; smtp_ssl = False                   ; ID: legacy option—tak perlu jika pakai smtp_encryption

smtp_user = look.haryoyok@gmail.com      ; ID: alamat Gmail/alias yang sah
smtp_password = lxknjvlbzkiibmdk    ; ID: 16 karakter app password TANPA spasi

; Opsional (pilih salah satu skenario):
; - Jika pakai SMTPS (465), ganti jadi:
; smtp_port = 465
; smtp_encryption = ssl_tls
